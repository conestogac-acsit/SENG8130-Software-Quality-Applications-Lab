export type Student = {
  studentId: string;
  name: string;
  email: string;
  group: string;
  role: string;
  loop: string;    
  github: string;  
  status: string;  
  loopStatus: string; 
  githubStatus: string;
};